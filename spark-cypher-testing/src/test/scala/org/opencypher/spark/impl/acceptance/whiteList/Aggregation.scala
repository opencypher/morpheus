package org.opencypher.spark.impl.acceptance.whiteList 
 
import org.scalatest.junit.JUnitRunner
import org.junit.runner.RunWith
import org.opencypher.spark.testing.CAPSTestSuite
import org.opencypher.spark.impl.acceptance.ScanGraphInit

                        @RunWith(classOf[JUnitRunner])
class AggregationTests extends CAPSTestSuite with ScanGraphInit {
//test for `max()` over integers 

//test for `min()` over mixed numeric values 

//test for `max()` over floats 

//test for `min()` over floats 

//test for `min()` over integers 
}