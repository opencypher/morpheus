
description = 'Graph DDL'

dependencies {
    compile project(':okapi-trees')
    compile project(':okapi-api')

    compile group: 'com.lihaoyi', name: "fastparse_$scalaVersion", version: '1.0.0'

    testCompile project(':okapi-testing')
}
